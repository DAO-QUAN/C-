# 🚨 常见错误（高频踩坑）

> 记不住的坑都在这，写代码前先扫一眼！

## 1️⃣ 拼写错误

```c
sancf   → scanf      // 少个 c
pintf   → printf     // 少个 f
gerdes  → grades     // 变量名拼错
date    → data       // 变量名拼错
```

> **解决：** 编译报错先看**拼写**，不要急着改逻辑！

## 2️⃣ 数组越界（最常犯！）

```c
int arr[5];  // 下标范围：0, 1, 2, 3, 4

for (int i = 0; i <= 5; i++)   // ❌ i=0,1,2,3,4,5 → 共6次，arr[5] 越界！
for (int i = 0; i < 5; i++)    // ✅ i=0,1,2,3,4    → 共5次，正确
```

> **记法：** 声明 `int arr[5]`，循环就用 `i < 5`，不要 `i <= 5`

## 3️⃣ 中文标点

```c
scanf("%d", &num)；   // ❌ 中文分号 ；→ 编译报错
printf("%d"，num);    // ❌ 中文逗号 ，→ 编译报错
```

> **所有符号都要用英文！** 最容易中招的是 `；` 和 `，`

## 4️⃣ scanf 放在循环外面

```c
// ❌ 只读一次，循环无限打印相同结果
scanf("%d", &num);
while (1) {
    if (num < 50) printf("小了");   // 永远都是同一个数
}

// ✅ 每次循环都重新读
while (1) {
    scanf("%d", &num);              // ✅ 在循环里面
    if (num < 50) printf("小了");
}
```

## 5️⃣ break 放在 else 外面

```c
// ❌ break 在 if-else 外面，第一次循环就退出
while (1) {
    if (条件) { ... }
    else { ... }
    break;          // ❌ 不管对不对都 break
}

// ✅ break 放在 else 里面
while (1) {
    if (条件) { ... }
    else { break; }  // ✅ 只有满足 else 才 break
}
```

## 6️⃣ 赋值方向搞反

```c
a + b = c;    // ❌ 左边必须是变量！
c = a + b;    // ✅ 正确：把 a+b 的结果赋给 c
```

> **记住：** 等号左边是"箱子"，右边是"要装进去的东西"

## 7️⃣ 整数除法忘转类型

```c
double c = 10 / 3;          // ❌ 10/3 = 3，c = 3.0
double c = (double)10 / 3;  // ✅ 10.0/3 = 3.333...
```

> 两个 `int` 相除 → 整数除法；**只要一个转成 `double` → 小数除法**

## 8️⃣ 先计算后输入

```c
int num;
int ge = num % 10;   // ❌ num 还没赋值！值是垃圾
scanf("%d", &num);

// ✅ 先输入，再计算
scanf("%d", &num);
int ge = num % 10;
```

## 9️⃣ NULL 不能用于 int

```c
int num;
if (num != NULL)     // ❌ NULL 是给指针用的，int 不能用！
if (scanf("%d", &num) == 1)  // ✅ 正确：判断 scanf 是否成功
```

## 🔟 `!==` 不存在

```c
if (a !== b)   // ❌ C 语言没有 !==
if (a != b)    // ✅ 不等于只有 !=
```

> C 里的"不等于"只有 `!=` 这一个写法

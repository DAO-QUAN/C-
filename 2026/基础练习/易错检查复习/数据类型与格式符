# 📦 数据类型与格式符

## 1️⃣ double — "能存小数的箱子"

| 类型 | 能存什么 | 例子 |
|------|----------|------|
| `int` | 整数 | 3, -5, 100 |
| `double` | 小数（精度高） | 3.14, -0.5, 99.9 |

```c
int    a = 10;        // 整数
double b = 3.14;      // 小数
```

> **为什么要有 double？** 因为 `int` 不能存小数，
> 比如 `10 / 3` 用 int 会得到 3（截断），
> 用 double 才得到 3.333...

## 2️⃣ % 格式符 — "告诉 printf 这是什么类型"

| 格式符 | 对应类型 | 示例 | 输出 |0
|--------|----------|------|------|
| `%d` | int | `printf("%d", 10)` | 10 |
| `%f` | double/float | `printf("%f", 3.14)` | 3.140000 |
| `%.2f` | double，保留2位小数 | `printf("%.2f", 3.14)` | 3.14 |
| `%c` | char（单个字符） | `printf("%c", 'A')` | A |
| `%s` | 字符串 | `printf("%s", "你好")` | 你好 |
| `%zu` | sizeof 返回的类型 | `printf("%zu", sizeof(int))` | 4 |

**记忆口诀：**
> `%d` → **d** = digit（数字）
> `%f` → **f** = float（小数）
> `%c` → **c** = char（字符）
> `%s` → **s** = string（字符串）

## 3️⃣ `(double)a / b` — 强制类型转换

```c
int a = 10, b = 3;
double c = a / b;          // ❌ 10/3 = 3（整数除法截断）
double c = (double)a / b;  // ✅ 10.0/3 = 3.333...
```

> 两个 int 相除会做整数除法。只要**其中一个转成 double**，整个除法就变成小数除法了。

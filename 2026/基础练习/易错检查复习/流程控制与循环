# 🚦 流程控制与循环

## 1️⃣ if / else if / else — "如果...否则如果...否则"

```c
if (条件1) {
    // 条件1成立时执行
}
else if (条件2) {
    // 条件1不成立，但条件2成立时执行
}
else {
    // 以上都不成立时执行
}
```

**执行规则：从上往下，碰到第一个成立的，执行完就跳过剩下的。**

```c
if (score >= 90)      printf("A");
else if (score >= 80) printf("B");   // 能走到这里，说明 score < 90
else if (score >= 70) printf("C");   // 能走到这里，说明 score < 80
else                  printf("F");   // 以上都不满足
```

## 2️⃣ switch — "多选一"

```c
switch (op) {
    case '+': c = a + b; break;
    case '-': c = a - b; break;
    case '*': c = a * b; break;
    case '/': c = (double)a / b; break;
    default: printf("无效运算符"); break;
}
```

> 每个 case 后面记得加 `break;`，否则会继续往下执行

## 3️⃣ for 循环 — "数到几就做几次"

```c
for (初始化; 条件; 步进) {
    // 重复执行的代码
}
```

**三个部分：**
```
for (int i = 1;    i <= 100;    i++) {
      ↑ 从1开始    ↑ 到100结束   ↑ 每次+1
}
```

**执行顺序：**
1. `int i = 1` → 只执行一次
2. 判断 `i <= 100` → 成立就执行循环体
3. 执行 `i++` → i 变成 2
4. 回到第2步...

## 4️⃣ for 嵌套 — "外层走一步，内层跑完整一遍"

```c
for (int i = 1; i <= 3; i++) {       // 外层
    for (int j = 1; j <= 2; j++) {   // 内层
        printf("i=%d j=%d  ", i, j);
    }
    printf("\n");                    // 每行结束换行
}
```

**输出：**
```
i=1 j=1  i=1 j=2
i=2 j=1  i=2 j=2
i=3 j=1  i=3 j=2
```

**执行过程：**
```
i=1 → 内层 j=1 → j=2 → 内层结束 → 换行
i=2 → 内层 j=1 → j=2 → 内层结束 → 换行
i=3 → 内层 j=1 → j=2 → 内层结束 → 换行
```

> 内层 `j` 每次都是从 1 重新开始！**外层管行数，内层管每行有几个。**

## 5️⃣ while 循环 — "当...时，反复做"

```c
while (条件) {
    // 只要条件成立，就一直重复
}
```

**猜数字例子：**
```c
while (1) {              // 1 永远成立 → 无限循环
    scanf("%d", &guess);
    if (guess == 50)
        break;           // 猜对了，跳出循环
}
```

## 6️⃣ break — "打断循环"

**`break` 放哪里？** → 放在你想退出的条件里面

```c
// ✅ 正确：猜对了才 break
while (1) {
    scanf("%d", &guess);
    if (guess == 50) {
        printf("对了！");
        break;         // ✅ 猜对才退出
    }
}

// ❌ 错误：不管对不对都 break
while (1) {
    scanf("%d", &guess);
    if (guess < 50) printf("小了");
    else if (guess > 50) printf("大了");
    else printf("对了");
    break;             // ❌ 第一次循环直接退出！
}
```

## 快速对比：while vs for

| 场景 | 用哪个 | 例子 |
|------|--------|------|
| 知道要循环几次 | `for` | 1加到100 |
| 不知道要循环几次 | `while` | 猜数字，直到猜对 |
| 至少要执行一次 | `do-while` | 菜单显示 |

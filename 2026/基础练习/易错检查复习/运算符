# 🔧 运算符

## 1️⃣ `%` 取余运算符

```c
10 % 3    → 1    （10 ÷ 3 = 3 余 1）
 8 % 2    → 0    （8 ÷ 2 整除，余 0）
 7 % 2    → 1    （7 ÷ 2 = 3 余 1）
```

**主要用途：判断奇偶**
```c
if (num % 2 == 0)   // 偶数
if (num % 2 == 1)   // 奇数
```

**拆数字：**
```c
int num = 789;
int ge  = num % 10;          // 个位：9
int shi = (num / 10) % 10;   // 十位：8
int bai = num / 100;         // 百位：7
```

## 2️⃣ `&&` 和 `||` — "且" 和 "或"

| 运算符 | 意思 | 例子 |
|--------|------|------|
| `&&` | **并且**（两个都要满足） | `score >= 90 && score <= 100` |
| `\|\|` | **或者**（满足一个就行） | `age < 18 \|\| age > 60` |

```c
// 判断闰年
if (year % 4 == 0 && (year % 100 != 0 || year % 400 == 0))
//     条件1 并且 （条件2 或者 条件3）
```

## 3️⃣ `+=` `-=` `*=` `/=` — 简写运算

| 简写 | 等价于 | 例子 |
|------|--------|------|
| `x += 5` | `x = x + 5` | `count += 1` → 计数加1 |
| `x -= 3` | `x = x - 3` | `hp -= 10` → 扣血 |
| `x *= 2` | `x = x * 2` | `score *= 2` → 分数翻倍 |
| `x /= 2` | `x = x / 2` | `n /= 2` → 减半 |
| `x %= 3` | `x = x % 3` | 取余后赋值 |

**记忆口诀：** 念作"x 加等于 5"、"x 减等于 3"

**⚠️ 注意：没有 `=+` 这种写法！**
```c
x += 1;    ✅ 正确：x = x + 1
x =+ 1;    ❌ 错误：这是把 +1 赋值给 x（其实是 x = 1）
```

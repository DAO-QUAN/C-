# 📌 指针入门

## 1️⃣ 核心概念

```
内存 = 一排带编号的箱子
变量 = 箱子里放的值
指针 = 箱子的编号（地址）
```

## 2️⃣ 两个最重要的符号

| 符号 | 名字 | 作用 |
|------|------|------|
| `&` | 取地址符 | 拿到变量的地址（箱子编号） |
| `*` | 解引用符 | 顺着地址找到变量（打开箱子） |

```c
int  a = 10;
int *p = &a;          // p 存了 a 的地址
printf("%d", *p);     // 输出 10（顺着 p 找箱子 = a 的值）
*p = 20;              // 改 *p 相当于改 a！
printf("%d", a);      // 输出 20
```

## 3️⃣ 声明指针

```c
int  *p;   // p 是一个指向 int 的指针
char *q;   // q 是一个指向 char 的指针
```

> `*` 在声明时表示"这是一个指针"，在使用时表示"解引用"

## 4️⃣ 指针交换变量

```c
void swap(int *a, int *b) {
    int temp = *a;     // temp = a 指向的值
    *a = *b;           // a 指向的值 = b 指向的值
    *b = temp;         // b 指向的值 = temp
}

// 调用
int x = 10, y = 20;
swap(&x, &y);          // 传地址！不是传值
// 现在 x=20, y=10
```

## 5️⃣ 指针与数组

```c
int arr[] = {10, 20, 30};

arr        == &arr[0]    // 数组名 = 首元素地址
*arr       == arr[0]     // *arr = 10
*(arr + 1) == arr[1]     // *(arr+1) = 20

// 用指针遍历数组
int *p = arr;
for (int i = 0; i < 3; i++) {
    printf("%d ", *(p + i));  // 或 p[i]
}
```

## 6️⃣ 函数用指针修改数组

```c
void double_values(int *arr, int size) {
    for (int i = 0; i < size; i++) {
        *(arr + i) *= 2;      // 每个元素翻倍
    }
}
```

## 🎯 口诀

> **`&` 取地址，`*` 解引用**
> **声明时 `*` 是说"我是指针"**
> **使用时 `*` 是说"取里面的值"**
